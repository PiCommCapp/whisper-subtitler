# HuggingFace API token for speaker diarization (required unless --no-diarization is used)
# Get your token from https://hf.co/settings/tokens and accept the terms at https://hf.co/pyannote/speaker-diarization
HUGGINGFACE_TOKEN=your-token-here

# Whisper model configuration 
WHISPER_MODEL_SIZE=medium  # Options: tiny, base, small, medium, large
WHISPER_LANGUAGE=en  # Language code or empty/auto for auto-detection
WHISPER_BEAM_SIZE=5  # Beam search width for Whisper
WHISPER_BEST_OF=5  # Number of samples for beam search
WHISPER_TEMPERATURE=0.0  # Temperature for sampling (0.0 for deterministic output)
WHISPER_INITIAL_PROMPT=  # Optional: initial prompt to guide transcription
AUTO_MODEL_SELECTION=false  # Set to true to automatically select optimal model
MODEL_SELECTION_CRITERIA=balanced  # Options: accuracy, speed, balanced
OPTIMIZE_FOR_AUDIO=false  # Set to true to optimize parameters based on audio type

# Speaker diarization settings
SKIP_DIARIZATION=false  # Set to true to disable speaker diarization
NUM_SPEAKERS=  # Optional: number of speakers if known (integer)
MIN_SPEAKERS=  # Optional: minimum number of speakers (integer)
MAX_SPEAKERS=  # Optional: maximum number of speakers (integer)
CLUSTER_SPEAKERS=false  # Set to true to enable speaker clustering for improved consistency
SIMILARITY_THRESHOLD=0.85  # Threshold for speaker clustering (0.0-1.0)
OPTIMIZE_NUM_SPEAKERS=false  # Set to true to optimize speaker identification when number is known
PREPROCESS_AUDIO=false  # Set to true to preprocess audio for improved diarization
VOICE_ACTIVITY_DETECTION=true  # Set to true to use voice activity detection
MIN_SILENCE_DURATION=0.3  # Minimum silence duration in seconds

# Audio processing settings
AUDIO_SAMPLE_RATE=42000  # Sample rate for extracted audio
AUDIO_CHANNELS=2  # Number of audio channels (1=mono, 2=stereo)
AUDIO_CONVERSION=true  # Set to true to convert audio to optimal format
AUDIO_NORMALIZATION=true  # Set to true to normalize audio volume
NOISE_REDUCTION=true  # Set to true to apply noise reduction
HIGH_PASS_FILTER=true  # Set to true to apply high-pass filter
HIGH_PASS_CUTOFF=80  # High-pass filter cutoff frequency (Hz)

# Output settings
OUTPUT_FORMATS=txt,srt,vtt,ttml  # Comma-separated list of formats to generate
TTML_TITLE=Transcription  # Title for TTML output
TTML_LANGUAGE=en-GB  # Language code for TTML output

# Performance settings
USE_CUDA=true  # Set to false to force CPU only
OMP_NUM_THREADS=4  # Number of threads for OpenMP
CUDA_VISIBLE_DEVICES=0  # GPU device ID to use (0 for first GPU)

# Logging settings
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR
SHOW_SPEAKER_DEBUG=false  # Set to true for detailed speaker segment logging
LOG_FILE=  # Optional: path to log file